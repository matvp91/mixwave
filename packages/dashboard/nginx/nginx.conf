server {
  listen 52000;
  
  location / {
    root /var/www;
    try_files $uri $uri/ /index.html;
    ssi on;
    set $PUBLIC_API_ENDPOINT "${PUBLIC_API_ENDPOINT}";
    set $PUBLIC_STITCHER_ENDPOINT "${PUBLIC_STITCHER_ENDPOINT}";
  }
}